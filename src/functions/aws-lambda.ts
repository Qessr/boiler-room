// import {} from "/src/types"
// import {} from "@aws-sdk/client-{service}"

// const awsServiceClient = new ServiceClient()

export const handler = async (event: any) => {
    try {
        
    } catch (err) {
        console.error(err)
        throw err
    }
}