import { handler } from './aws-lambda';
import { EventBridgeEvent } from 'aws-lambda';

describe('aws-lambda', () => {
    beforeEach(() => {
        jest.clearAllMocks()
    })

    describe('given x', () => {
        it ('should y', async () => {

        })
    })
})